# OS
.DS_Store
Thumbs.db

# Claude Code session state (local to the machine, not project content)
/.claude

# Editors
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Laravel (app root)
*.log
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.phpunit.cache
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
/database/database.sqlite
Homestead.json
Homestead.yaml

# Frontend production build output (Vite's outDir points here — see
# frontend/vite.config.ts and docs/02-system-architecture.md "Production
# Deployment"). Forge rebuilds this on every deploy; never commit it.
/public/assets
/public/index.html
/public/manifest.webmanifest
/public/registerSW.js
/public/sw.js
/public/workbox-*.js
/public/favicon.svg
/public/icon.svg
/public/icons.svg

# Docker-mounted volumes are declared in docker-compose.yml, not tracked here.

# One-off build inputs for scripts/tajweed-build/remap.py — re-download from
# the URLs documented in that script's docstring if regenerating
# database/data/quran-tajweed.json (which IS committed).
/scripts/tajweed-build/source-*
__pycache__/
